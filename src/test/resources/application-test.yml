themealdb:
    protocol: http
    host: localhost
    port: 8090
    basepath: api/json/v1
    apikey: 1

logging:
    level:
        org:
            springframework:
                web:
                    reactive:
                        function:
                            client:
                                ExchangeFunctions: TRACE
        reactor:
            netty:
                http:
                    client:
                        HttpClient: DEBUG